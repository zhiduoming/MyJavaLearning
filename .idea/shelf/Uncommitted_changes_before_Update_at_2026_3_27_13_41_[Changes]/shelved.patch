Index: .idea/.gitignore
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/.idea/.gitignore b/.idea/.gitignore
new file mode 100644
--- /dev/null	(date 1773856564478)
+++ b/.idea/.gitignore	(date 1773856564478)
@@ -0,0 +1,3 @@
+# Default ignored files
+/shelf/
+/workspace.xml
Index: JavaBasic/Notes/JavaBasic.md
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/JavaBasic/Notes/JavaBasic.md b/JavaBasic/Notes/JavaBasic.md
new file mode 100644
--- /dev/null	(date 1773856564481)
+++ b/JavaBasic/Notes/JavaBasic.md	(date 1773856564481)
@@ -0,0 +1,140 @@
+
+# 如梦初醒
+
+## 2025.3.16打卡 Day 1
+
+### 学习JavaSE基础
+- 学习了方法的定义及调用
+
+>方法要定义在main方法的里面，类的外面，方法在类中的位置放前放后无所谓，但一个方法不能定义在另一个方法里面。
+
+    方法定义的基本形式：
+```java
+    修饰符 返回值类型  方法名（数据类型 形参）{
+        方法体代码；
+        return 返回值；
+    }
+```
+<img src="picture/img_1.png" width="400">    <img src="picture/img_2.png" width="400" height="230">
+
+
+方法的调用:
+`int result=sum(n);`（有返回值）`print();`(无返回值)
+
+- 方法的重载(不同方法使用相同的方法名)
+>其特征：形参的个数、类型、顺序不同，不关心形参的名称（且与返回值类型是什么无关）
+
+```java
+public static  int getSum(int a,int b){
+        return a+b;
+    }
+    public static  int getSum( int a,int b,int c){
+        return a+b+c;
+    }
+    public static double getSum(double a,double b){
+        return a+b;
+    }
+```
+调用时只需传参作以区分：
+```java
+        int result =getSum(13 ,45);
+        double result2 =getSum(1.4,4.3);
+        int  result3= getSum(4,6,8);
+```
+- java中内存的分配：
+>栈内存（方法执行），方法区（类信息、静态变量、常量），堆内存（new关键字），程序计数器（当前线程执行的字节码行号），本地方法栈（Native 方法服务）
+
+- 类与对象注意事项：
+> 类名建议用英文单词，首字母大写，满足驼峰模式，且要有意义，比如：Student、Car
+
+>类中定义的变量也称为成员变量（对象的属性），类中定义的方法也称为成员方法（对象的行为）
+
+> 成员变量本身存在默认值，在定义成员变量时一般来说不需要赋初始值
+
+> 一个代码文件中，可以写多个class类，但只能一个用public修饰，且public修饰的类名必须成为代码文件名。
+
+>对象与对象之间的数据不会相互影响，但多个变量指向同一个对象时就会相互影响了。
+
+>如果某个对象没有一个变量引用它，则该对象无法被操作了，该对象会成为所谓的垃圾对象。
+- 封装
+>就是用类设计对象处理某一个事物的数据时，应该把要处理的数据，以及处理这些数据的方法，设计到一个对象中去。
+
+>合理隐藏，合理暴露:  
+> 公开成员，可以使用public(公开)进行修饰。  
+隐藏成员，使用private(私有，隐藏) 进行修饰
+
+- `private`关键字：
+> 可见性：被 private 修饰的成员（变量、方法、构造器）只能在定义它的类内部被访问。
+
+>对外部隐藏：无论是子类（继承）、同包下的其他类，还是外部完全无关的类，都无法直接看到或调用 private 成员
+
+- `this`关键字与就近原则：
+> this就是一个变量，可以用在方法中，来拿到当前对象，
+> 当成员变量名与方法内局部变量名冲突时，使用this.变量名相当于this保存了这个实例对象在堆的地址，
+> 然后拿到该对象的成员变量，可以起到区分变量的作用，只要使用this关键字，就会指向其内部的成员变量。
+
+> 当一个作用域内出现了多个同名变量时，程序会优先使用距离当前执行位置“最近”的那一个。
+
+- 构造方法：
+>分两类，一种是不带参数的构造方法，另一种是带全部参数的构造方法。如果不写构造方法，则系统会自动生成一个不带参数的构造方法
+> 在以后的学习过程中需要两种都写，但可以使用快捷键`alt+insert`来产生构造方法，或者使用ptg插件，鼠标右键之后直接创建。
+```java
+//不带参数的构造方法：
+public User() {
+}
+
+//带全部参数的构造方法：
+ public User(String username, String password, String email, String gender, int age) {
+        this.username = username;
+        this.password = password;
+        this.email = email;
+        this.gender = gender;
+        this.age = age;
+    }
+```
+- 成员变量和局部变量
+  <img src="picture/img_4.png" width="800">
+
+- API
+> 全称（Application Programming Interface：应用程序编程接口）
+> API是别人写好的东西，我们不需要去自己编写，直接拿来使用即可。例如：Scanner,String,Random等
+
+- API帮助文档：
+> [jdk-17中文api.CHM](../../Users/ASUS/Desktop/jdk-17%E4%B8%AD%E6%96%87api.CHM)
+
+-字符串
+> 在字符串比较中，基本数据类型比较的是数据值，引用数据类型比较多是地址的值
+
+```java 
+String s1= new String("abc");
+String s2= new String("abc");
+System.out.println(s1==s2);
+//打印的结果为false，因为s1和s2都存的是地址，地址不相同；
+```
+>只要是以“...”方式写出的字符串对象，会存储到字符串常量池，且相同内容的字符串只存储一份；
+但通过new方式创建字符串对象，每new一次都会产生一个新的对象放在堆内存中
+>一般采用直接赋值的方法来获取一个字符串对象
+
+`例如：String s1="abc"；如果直接赋值时不知道赋什么值可以：String s1=""；`
+
+- equals方法和equalsIgnoreCase方法用来比较字符串的内容
+>System.out.println(s2.equals(s3));//不忽略大小写
+System.out.println(s2.equalsIgnoreCase(s3));//忽略大小写
+
+
+<img src="picture/img.png" width="800">
+
+
+
+
+#### 总结：今天收获满满，也动手敲了很多代码，感觉光听别人讲，光看别人总结是学不会的，实践是检验真理的唯一标准，用gemini对我的分析：行动是治愈一切焦虑的唯一良药。以此来记录我的后端学习历程
+
+
+
+
+
+
+
+
+
+
Index: .gitignore
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/.gitignore b/.gitignore
new file mode 100644
--- /dev/null	(date 1773856564478)
+++ b/.gitignore	(date 1773856564478)
@@ -0,0 +1,38 @@
+target/
+!.mvn/wrapper/maven-wrapper.jar
+!**/src/main/**/target/
+!**/src/test/**/target/
+
+### IntelliJ IDEA ###
+.idea/modules.xml
+.idea/jarRepositories.xml
+.idea/compiler.xml
+.idea/libraries/
+*.iws
+*.iml
+*.ipr
+
+### Eclipse ###
+.apt_generated
+.classpath
+.factorypath
+.project
+.settings
+.springBeans
+.sts4-cache
+
+### NetBeans ###
+/nbproject/private/
+/nbbuild/
+/dist/
+/nbdist/
+/.nb-gradle/
+build/
+!**/src/main/**/build/
+!**/src/test/**/build/
+
+### VS Code ###
+.vscode/
+
+### Mac OS ###
+.DS_Store
\ No newline at end of file
